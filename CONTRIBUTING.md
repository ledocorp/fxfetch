# Contributing

1. Keep changes fx-native (`fx build … --cli`; no per-tool host.c).
2. Dual-path: emit-C and IR corpora must stay green before release.
3. NetCap `--allow-host` and `--ca` stay mandatory for HTTPS.
4. Public docs stay free of private board jargon.

License: Apache-2.0.
